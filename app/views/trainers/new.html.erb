<h1>Trainers#new</h1>
<p> <%= notice %> </p>

<%= form_with(model: @trainer, method: :post) do |form| %>
    <div class="field">
        <%=form.label :nombre%>
        <%=form.text_field :nombre%>
    </div>

    <div class="action">
        <%=form.submit "Crear" %>
    </div>
<% end %>